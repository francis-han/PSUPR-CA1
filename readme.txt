Pre-requisites

Anaconda with Python 3.7

using psupr environment with additional packages

1) pip install eli5
2) pip install hyperopt
3) conda install seaborn

Submission:

1) Python code written in Jupyter Notebook
2) PPT file - project report
